---
phase: 02-people-and-items-ui
plan: 2
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/App.tsx
  - src/components/ItemsPanel.tsx
  - src/components/ItemRow.tsx
  - src/components/AssignmentChips.tsx
  - src/components/__tests__/ItemsPanel.test.tsx
  - src/components/__tests__/ItemRow.test.tsx
autonomous: true
requirements:
  - ITEMS-01
  - ITEMS-02
  - ITEMS-03
  - ITEMS-04
must_haves:
  truths:
    - User can add a receipt item with a name and dollar price
    - User can edit an item's name and price inline
    - User can delete an item
    - User can assign an item to specific people via tap-toggle chips
    - User can mark an item as shared among a chosen subset of people
    - Unassigned items (splitMode=assigned, assignedTo=[]) show amber warning border and label
    - Assignment chips are hidden when no people exist, with hint text shown instead
  artifacts:
    - path: src/components/ItemsPanel.tsx
      provides: Items section with add form and item list
      exports:
        - ItemsPanel
    - path: src/components/ItemRow.tsx
      provides: Single item row with inline edit, delete, split mode toggle, and assignment chips
      exports:
        - ItemRow
    - path: src/components/AssignmentChips.tsx
      provides: Inline person-toggle chip buttons for item assignment
      exports:
        - AssignmentChips
    - path: src/components/__tests__/ItemsPanel.test.tsx
      provides: Component tests for ITEMS-01, ITEMS-02
    - path: src/components/__tests__/ItemRow.test.tsx
      provides: Component tests for ITEMS-03, ITEMS-04
  key_links:
    - from: src/components/ItemsPanel.tsx
      to: src/store/billStore.ts
      via: useBillStore selectors for items, addItem
      pattern: useBillStore\(.*state.*items
    - from: src/components/ItemRow.tsx
      to: src/store/billStore.ts
      via: useBillStore selectors for updateItem, removeItem, people
      pattern: useBillStore\(.*state.*(updateItem|removeItem|people)
    - from: src/components/ItemRow.tsx
      to: src/components/AssignmentChips.tsx
      via: renders AssignmentChips with people and assignedTo
      pattern: <AssignmentChips
    - from: src/components/AssignmentChips.tsx
      to: src/store/billStore.ts
      via: toggle updates item assignedTo via updateItem
      pattern: updateItem
    - from: src/App.tsx
      to: src/components/ItemsPanel.tsx
      via: renders ItemsPanel in main layout
      pattern: <ItemsPanel
    - from: src/utils/parseDollars.ts
      to: src/components/ItemsPanel.tsx
      via: parseDollarsToCents called on item add and price edit
      pattern: parseDollarsToCents
---
<objective>
Build the complete Items panel with add/edit/delete item functionality, inline person-assignment chips with tap-toggle, split mode switching (shared vs assigned), and unassigned item visual flagging. Wire ItemsPanel into App.tsx.

Purpose: Completes the full input surface for Phase 2 -- users can enter all people and all items, assign who had what, and see which items still need assignment.
Output: Working Items panel with assignment chips, all component tests passing.
</objective>

<execution_context>
@/Users/sandrap/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sandrap/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-people-and-items-ui/02-RESEARCH.md
@.planning/phases/02-people-and-items-ui/02-UI-SPEC.md
@.planning/phases/02-people-and-items-ui/02-VALIDATION.md
@.planning/phases/02-people-and-items-ui/02-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/types/models.ts:
```typescript
export interface Person {
  id: string;
  name: string;
}

export interface Item {
  id: string;
  description: string;
  priceInCents: number;
  splitMode: 'shared' | 'assigned';
  assignedTo: string[];
}
```

From src/store/billStore.ts:
```typescript
addItem: (description: string, priceInCents: number) => void;
removeItem: (id: string) => void;
updateItem: (id: string, updates: Partial<Item>) => void;
// addItem defaults: splitMode: 'shared', assignedTo: []
```

From src/utils/parseDollars.ts (created in Plan 01):
```typescript
export function parseDollarsToCents(input: string): number | null;
// Returns null for invalid input (NaN, negative). Returns integer cents otherwise.
```

From src/utils/formatCents.ts (created in Plan 01):
```typescript
export function formatCents(cents: number): string;
// Returns "$X.XX" formatted string.
```

From src/components/PeoplePanel.tsx (created in Plan 01):
```typescript
export function PeoplePanel(): JSX.Element;
// Already rendered in App.tsx
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build ItemsPanel, ItemRow, and AssignmentChips components with full item CRUD and person assignment</name>
  <files>src/components/ItemsPanel.tsx, src/components/ItemRow.tsx, src/components/AssignmentChips.tsx, src/App.tsx</files>
  <read_first>
  - src/store/billStore.ts (store API: addItem, removeItem, updateItem signatures and defaults)
  - src/types/models.ts (Item and Person interfaces)
  - src/utils/parseDollars.ts (parseDollarsToCents signature)
  - src/utils/formatCents.ts (formatCents signature)
  - src/App.tsx (current state -- need to add ItemsPanel import)
  - src/components/PeoplePanel.tsx (reference for patterns: Zustand selectors, inline edit, styling conventions)
  - .planning/phases/02-people-and-items-ui/02-UI-SPEC.md (ItemsPanel, ItemRow, AssignmentChips visual contracts, copywriting, interaction contracts)
  - .planning/phases/02-people-and-items-ui/02-RESEARCH.md (Pattern 2: chip toggle, Pattern 4: dollar-to-cents parsing, Pattern 5: unassigned flagging)
  </read_first>
  <action>
1. Create `src/components/AssignmentChips.tsx`:
  - Props: `people: Person[]`, `assignedTo: string[]`, `onToggle: (personId: string) => void`
  - Render a `<div className="flex flex-wrap gap-1.5">` containing one `<button>` per person
  - Each button: `key={person.id}`, `onClick={() => onToggle(person.id)}`, `aria-pressed={isAssigned}`
  - Active chip className: `"min-h-[44px] min-w-[44px] px-3 py-1.5 rounded-full text-sm font-medium transition-colors bg-blue-500 text-white"`
  - Inactive chip className: `"min-h-[44px] min-w-[44px] px-3 py-1.5 rounded-full text-sm font-medium transition-colors bg-gray-100 text-gray-600 dark:bg-gray-700 dark:text-gray-300"`
  - Button text: `person.name`
  - Export as named export: `export function AssignmentChips({ people, assignedTo, onToggle }: Props)`

2. Create `src/components/ItemRow.tsx`:
  - Props: `item: Item`
  - Read from store: `const people = useBillStore((s) => s.people)`, `const updateItem = useBillStore((s) => s.updateItem)`, `const removeItem = useBillStore((s) => s.removeItem)`
  - State for inline editing description: `const [editingDesc, setEditingDesc] = useState(false)`, `const [draftDesc, setDraftDesc] = useState(item.description)`
  - State for inline editing price: `const [editingPrice, setEditingPrice] = useState(false)`, `const [draftPrice, setDraftPrice] = useState(formatCents(item.priceInCents).replace('$', ''))` -- display without dollar sign in edit mode
  - Compute `isUnassigned = item.splitMode === 'assigned' && item.assignedTo.length === 0`
  - Card wrapper: `<div className={\`border rounded-lg p-3 \${isUnassigned ? 'border-amber-400 bg-amber-50 dark:bg-amber-900/20' : 'border-gray-200 dark:border-gray-700'}\`}>`
  - First line: flex with description (left, click-to-edit) and price (right, click-to-edit) and delete button
  - Description inline edit: same pattern as PersonRow (click to edit, Enter saves via `updateItem(item.id, { description: draftDesc.trim() })`, Escape cancels)
  - Price inline edit: on click, show `<input inputMode="decimal" ...>`. On Enter/blur, parse with `parseDollarsToCents(draftPrice)`. If result is not null, call `updateItem(item.id, { priceInCents: result })`. If null, show inline error text "Enter a valid price (e.g. 12.50)" and keep input open. Display `formatCents(item.priceInCents)` when not editing.
  - Delete button: text or icon, `onClick={() => removeItem(item.id)}`, destructive style `"text-red-500 min-h-[44px]"`
  - Split mode toggle: a row with two text buttons "Shared" and "Assigned". Active mode gets `"font-medium text-blue-500"`, inactive gets `"text-gray-500"`. Clicking "Shared" calls `updateItem(item.id, { splitMode: 'shared' })`. Clicking "Assigned" calls `updateItem(item.id, { splitMode: 'assigned' })`.
  - Assignment chips row: if `people.length > 0`, render `<AssignmentChips people={people} assignedTo={item.assignedTo} onToggle={handleToggle} />`. The `handleToggle(personId)` function: if personId is in assignedTo, remove it; if not, add it. Call `updateItem(item.id, { assignedTo: newAssignedTo })`.
  - If `people.length === 0`, show `<p className="text-sm text-gray-500">Add people first to assign items</p>`
  - Unassigned warning: if `isUnassigned`, show `<span className="text-xs text-amber-600 font-medium">Not assigned to anyone</span>`
  - Export as named export: `export function ItemRow({ item }: { item: Item })`

3. Create `src/components/ItemsPanel.tsx`:
  - Read from store: `const items = useBillStore((s) => s.items)`, `const addItem = useBillStore((s) => s.addItem)`
  - Local state: `const [description, setDescription] = useState('')`, `const [price, setPrice] = useState('')`, `const [priceError, setPriceError] = useState('')`
  - `handleAdd` function: validate description.trim() is non-empty and parseDollarsToCents(price) is not null. If price invalid, set priceError to "Enter a valid price (e.g. 12.50)". Otherwise call `addItem(description.trim(), parsedCents)`, clear inputs, clear error, refocus description input (useRef).
  - Section heading: `<h2 className="text-[20px] font-bold leading-[1.2]">Items</h2>`
  - Add form: description input (placeholder="Item name") + price input (placeholder="$0.00", `inputMode="decimal"`) + "Add Item" button
  - Description input className: `"flex-1 border border-gray-300 dark:border-gray-600 rounded-lg px-3 py-2 text-base bg-white dark:bg-gray-800 dark:text-gray-100"`
  - Price input className: same but with `"w-24"` for narrower width
  - Button className: `"bg-blue-500 text-white rounded-lg px-4 py-2 min-h-[44px]"`
  - If priceError is set, show `<p className="text-sm text-red-500">{priceError}</p>` below the form
  - Item list: `<div className="space-y-4">` mapping `items.map(item => <ItemRow key={item.id} item={item} />)`
  - Empty state when items.length === 0: `<p className="text-sm text-gray-500">No items yet. Add receipt items above.</p>`
  - Handle Enter key on price input to trigger handleAdd
  - Export as named export: `export function ItemsPanel()`

4. Update `src/App.tsx`: add `import { ItemsPanel } from './components/ItemsPanel'` and add `<ItemsPanel />` after `<PeoplePanel />` inside main.
  </action>
  <verify>
    <automated>cd /Users/sandrap/Downloads/gsd-module-test && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
  - `grep -q 'export function ItemsPanel' src/components/ItemsPanel.tsx` exits 0
  - `grep -q 'export function ItemRow' src/components/ItemRow.tsx` exits 0
  - `grep -q 'export function AssignmentChips' src/components/AssignmentChips.tsx` exits 0
  - `grep -q 'parseDollarsToCents' src/components/ItemsPanel.tsx` exits 0
  - `grep -q 'formatCents' src/components/ItemRow.tsx` exits 0
  - `grep -q 'aria-pressed' src/components/AssignmentChips.tsx` exits 0
  - `grep -q 'min-h-\[44px\]' src/components/AssignmentChips.tsx` exits 0
  - `grep -q 'border-amber-400' src/components/ItemRow.tsx` exits 0
  - `grep -q 'Not assigned to anyone' src/components/ItemRow.tsx` exits 0
  - `grep -q 'Add people first to assign items' src/components/ItemRow.tsx` exits 0
  - `grep -q 'No items yet' src/components/ItemsPanel.tsx` exits 0
  - `grep -q 'Item name' src/components/ItemsPanel.tsx` exits 0
  - `grep -q 'inputMode.*decimal' src/components/ItemsPanel.tsx` exits 0
  - `grep -q 'Shared' src/components/ItemRow.tsx` exits 0
  - `grep -q 'Assigned' src/components/ItemRow.tsx` exits 0
  - `grep -q 'ItemsPanel' src/App.tsx` exits 0
  - `npm run build` exits 0
  </acceptance_criteria>
  <done>ItemsPanel renders "Items" heading, add-item form (description + price + button), item list, and empty state. ItemRow shows item name and price with inline edit, delete button, split mode toggle (Shared/Assigned), assignment chips, and unassigned warning. AssignmentChips renders tap-toggle chips with 44px touch targets and aria-pressed. App.tsx renders both PeoplePanel and ItemsPanel. Build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Write component tests for ItemsPanel (add/edit/delete items) and ItemRow (assignment chips, shared mode, unassigned flagging)</name>
  <files>src/components/**tests**/ItemsPanel.test.tsx, src/components/**tests**/ItemRow.test.tsx</files>
  <read_first>
  - src/components/ItemsPanel.tsx (component under test)
  - src/components/ItemRow.tsx (component under test)
  - src/components/AssignmentChips.tsx (used within ItemRow)
  - src/store/billStore.ts (store reset pattern for beforeEach)
  - src/components/**tests**/PeoplePanel.test.tsx (reference: test patterns, store reset, userEvent setup)
  - .planning/phases/02-people-and-items-ui/02-VALIDATION.md (test map: which requirements map to which test files)
  </read_first>
  <action>
1. Create `src/components/__tests__/ItemsPanel.test.tsx`:
  - Import render, screen from @testing-library/react, userEvent from @testing-library/user-event, ItemsPanel, useBillStore
  - `beforeEach`: reset store with `useBillStore.setState({ people: [], items: [], settings: { defaultTipPercent: 18, defaultTaxPercent: 0 } })`
  - Test "shows empty state when no items" (ITEMS-01): render ItemsPanel, expect `screen.getByText(/no items yet/i)` in document
  - Test "adds an item with name and price" (ITEMS-01): render, type "Burger" in input with placeholder /item name/i, type "12.50" in input with placeholder "$0.00", click "Add Item", expect "Burger" in document, expect "$12.50" in document, expect inputs cleared
  - Test "adds item on Enter in price field" (ITEMS-01): render, type "Fries" in name input, type "5.99" in price input, press Enter on price input, expect "Fries" and "$5.99" in document
  - Test "shows error for invalid price" (ITEMS-01): render, type "Salad" in name input, type "abc" in price input, click "Add Item", expect `screen.getByText(/enter a valid price/i)` in document
  - Test "does not add item with empty name": render, type "10.00" in price input, click "Add Item", expect empty state still shown
  - Test "deletes an item" (ITEMS-02): seed store with one item via `useBillStore.getState().addItem('Burger', 1250)`, render, click delete button (find by role or test-id), expect "Burger" NOT in document

2. Create `src/components/__tests__/ItemRow.test.tsx`:
  - Import render, screen from @testing-library/react, userEvent from @testing-library/user-event, ItemRow, useBillStore
  - `beforeEach`: reset store with people ["Alice", "Bob"] and one item "Burger" at 1250 cents. Use `useBillStore.getState().addPerson('Alice')` and `useBillStore.getState().addPerson('Bob')` and `useBillStore.getState().addItem('Burger', 1250)`. Then read the item from store to pass as prop.
  - Test "renders item name and formatted price": render `<ItemRow item={item} />`, expect "Burger" and "$12.50" in document
  - Test "edits item description inline" (ITEMS-02): render, click on "Burger" text, clear and type "Cheeseburger", press Enter, verify store state has updated description via `useBillStore.getState().items[0].description === 'Cheeseburger'`
  - Test "edits item price inline" (ITEMS-02): render, click on "$12.50" text, clear and type "15.00", press Enter, verify store state has `priceInCents === 1500`
  - Test "assigns a person via chip toggle" (ITEMS-03): render, expect chips for "Alice" and "Bob", click "Alice" chip, verify store `items[0].assignedTo` includes Alice's id
  - Test "unassigns a person by toggling chip off" (ITEMS-03): pre-assign Alice to the item in store, render, click "Alice" chip, verify store `items[0].assignedTo` does NOT include Alice's id
  - Test "shows shared/assigned toggle" (ITEMS-04): render, expect "Shared" and "Assigned" text visible, click "Assigned", verify store `items[0].splitMode === 'assigned'`
  - Test "shows unassigned warning when splitMode=assigned and no one assigned" (ITEMS-04): set item to `splitMode: 'assigned'` and `assignedTo: []` in store, render, expect `screen.getByText(/not assigned to anyone/i)` in document
  - Test "hides unassigned warning when someone is assigned" (ITEMS-04): set item to `splitMode: 'assigned'` with Alice assigned, render, expect `screen.queryByText(/not assigned to anyone/i)` to be null
  - Test "shows hint when no people exist" (ITEMS-03): reset store to have zero people but one item, render, expect `screen.getByText(/add people first/i)` in document
  </action>
  <verify>
    <automated>cd /Users/sandrap/Downloads/gsd-module-test && npx vitest run src/components/**tests**/ItemsPanel.test.tsx src/components/**tests**/ItemRow.test.tsx --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c 'test\|it(' src/components/__tests__/ItemsPanel.test.tsx` returns at least 5 (five or more test cases)
  - `grep -c 'test\|it(' src/components/__tests__/ItemRow.test.tsx` returns at least 7 (seven or more test cases)
  - `grep -q 'ITEMS-01\|add.*item\|adds.*item' src/components/__tests__/ItemsPanel.test.tsx` exits 0
  - `grep -q 'ITEMS-02\|delete\|edit' src/components/__tests__/ItemsPanel.test.tsx` exits 0
  - `grep -q 'ITEMS-03\|assign' src/components/__tests__/ItemRow.test.tsx` exits 0
  - `grep -q 'ITEMS-04\|shared\|unassigned' src/components/__tests__/ItemRow.test.tsx` exits 0
  - `npx vitest run src/components/__tests__/ItemsPanel.test.tsx` exits 0
  - `npx vitest run src/components/__tests__/ItemRow.test.tsx` exits 0
  - `npx vitest run` exits 0 (full suite including Phase 1 tests still passes)
  </acceptance_criteria>
  <done>ItemsPanel tests cover add item (by click and Enter), invalid price error, empty name guard, and delete item. ItemRow tests cover inline edit of name and price, chip toggle assign/unassign, split mode switching, unassigned warning display/hide, and no-people hint. All tests pass. Full test suite (including Phase 1 engine tests) remains green.</done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds with zero errors
2. `npx vitest run --reporter=verbose` -- all tests pass (Phase 1 engine tests + Phase 2 component tests)
3. `npm run dev` -- app renders "Split the Bill" with People panel and Items panel, both fully functional
4. Items with splitMode=assigned and empty assignedTo show amber border + "Not assigned to anyone" warning
5. Assignment chips show all people as toggleable buttons with 44px touch targets
</verification>

<success_criteria>
- User can add an item with name and dollar price (ITEMS-01)
- User can edit an item's name and price inline, and delete an item (ITEMS-02)
- User can assign an item to one or more specific people via tap-toggle chips (ITEMS-03)
- User can toggle between shared and assigned split modes, with subset selection via chips (ITEMS-04)
- Unassigned items show amber visual warning
- All component tests pass
- Full test suite green (Phase 1 + Phase 2)
- Build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/02-people-and-items-ui/02-02-SUMMARY.md`
</output>
